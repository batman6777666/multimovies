{
  "name": "video-embed-link-extractor",
  "version": "2.0.0",
  "description": "Blazing fast video embed link extractor API",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "build": "echo 'No build step required'",
    "build:frontend": "npm install --prefix frontend && npm run --prefix frontend build && cp -r frontend/dist ./out"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.0",
    "express": "^4.18.2",
    "express-rate-limit": "^7.0.0",
    "puppeteer": "^23.0.0",
    "redis": "^4.6.0"
  }
}